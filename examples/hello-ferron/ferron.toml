format_version = 1

[project]
name = "hello-ferron"
version = "0.1.0"

[scripts]
dir = "scripts"
entry = "HelloFerron.Main, HelloFerron"

[assets]
dir = "assets"

# Uncomment once the scene format lands (issue #6 / scene format):
# [scenes]
# start = "scenes/main.fscene"
